# TCR：A—H 开发与聚焦运行验证

> 本地实现、构建与基础运行回执；不代表完整自动化测试或发布。

> 后续修正：用户日常数据目录曾因 Session schema 7 / 8 硬切启动失败。最新指令和修复结果见 [历史会话保留与 GUI 启动修复](TCR-20260915-session-startup.md)：已去除 Session 版本相等门禁、保留旧历史，补齐实际默认目录与 Finder 启动验证。下文保留 A—H 原始验证时点的记录。

## 1. 范围与有效约定

- 依据外层《开发规划方案》和本次 A—H 批次授权实施。
- 用户已要求关闭子代理；后续由主对话单独完成。
- 核心采用真实 CLI / Provider / Kernel 链路验证；GUI 只做启动和简单插件、页面检查，不增加复杂测试脚本。用户再次授权自主完成基础测试，无法完成的非关键检查可标注后跳过。
- 最新交互决定：产物列表不按当前轮或历史轮筛选，显示整个 Session 的交付版本，按最后内容变更对应的执行时间从新到旧排列。固定交付版本不随当前文件或页面变化而改写。
- 本次不修改锁定规划文档，不提交、不建 PR、不合并、不发布。独立后置的 VS Code 专项、TUI 专项体验、child Session、Chromium 扩展、模型自动视觉审校不在本次主线范围内。

## 2. 工作区与实现归属

- 工作区：`DeepCode-dev-main`；分支：`session/document-skill-preview`；基线：`0369515`。
- 工作区开始时已有大量未提交修改，包括部分测试文件。保留原有修改；下述实现为本次工作与已有基础的集成状态，不把全部 dirty diff 归为本次新增。
- Kernel 负责工具执行、权限、真实 fileChanges、固定 artifact 存储和调用绑定。
- Session 负责同一 run 的请求工具视图、输入接纳、历史调用别名、释放和唯一投影。
- Host 负责原生子 WebView、精确窗口绑定和开发进程资源；GUI 负责分类设置、引用、Reader、产物列表和插件展示。
- 当前 Session schema 为 8；未新增旧数据迁移。验证使用独立配置和数据目录，未改动用户原始会话库。

## 3. 变更覆盖

| 批次 | 本次实现 | 验证状态 |
| --- | --- | --- |
| A | 统一插件目录与结构化引用；默认隐藏可搜索的产品资料；内置文档指导可发现；选择、准备和可用状态区分 | CLI 目录、指导引用、选择和未选择坏插件的基础对话已通过 |
| B | 功能插件 / UI 扩展 / 自定义 Skill 分类，逐项保存；实际使用模型决定新对话默认；Reader 支持文档、PDF、图片和浏览器；模型用量归入模型与服务 | 类型检查、GUI 分类设置和 Session 产物阅读已通过；PDF 交互矩阵跳过 |
| C | macOS WKWebView 子视图创建、布局、显隐、焦点、同页捕获与关闭 | 真实 HTML/CSS/JS、输入、点击、布局、同页截图和关闭通过 |
| D | 同一 run 接纳明确新增工具；下一请求更新定义、指导、别名和绑定；旧请求按原绑定解释，Kernel 保留多个视图并释放 | 真实 CLI + MCP + Provider 已通过 |
| E | 精确 Host / Session 页面绑定；本地 HTML 相对资源、已有 URL、开发服务、自身 GUI 预览；统一 Kernel 执行 | 本地文件、真实开发 URL、服务启动/停止、自身预览通过 |
| F | artifact 的 session/run/call/record/媒体类型关联；固定归档读取；真实执行前后内容和修改清单；整个 Session 的产物排序 | 两版文档、真实 diff、截图、内容变化后的历史固定读取通过 |
| G | 单 UI surface 构建；独立插件模板；真实展示插槽与更新/dispose；草稿和阅读状态保留 | 单插件构建和真实 GUI 热更新通过 |
| H | Docker 通用构建与 macOS 原生打包；CLI 真实对话，TUI / GUI 启动 | macOS 构建与真实 CLI / TUI / GUI 基础运行通过；最终包身份见下文 |

## 4. 测试资产变更说明

本次主对话没有新增、删除、放宽或取消注册测试，没有编写复杂验证 runner、mock 或固定问句生产分支。`examples/plugins/echo` 是可运行的 MCP 开发示例，`ui-plugins/template` 是 UI 插件模板，均作为产品开发入口使用。

工作区原有 `test.sh`、`scripts/tests/*`、GUI Session contract 测试的未提交改动保留，不能据此声称本次新改了这些测试。按用户要求，本次未执行完整 `test.sh` 自动化套件；构建、类型检查、架构检查与下述真实运行分别记账。

## 5. 已获得的运行证据

### 同一任务动态添加 MCP 工具

- Session：`session:19fd9a629bdc795c5d8a551a8f3898fd`
- 唯一 run：`session:19fd9a629bdc795c5d8a551a8f3898fd:run:mu2f7xev:2`
- 前两次上下文没有 Echo；交互等待中使用 CLI `--plugin plugin://echo-example@mcp` 补充输入。
- `run.tools.prepared` 序号 26；后续上下文序号 29 / 38 使用新目录。
- 实际 `mcp.echo-example.echo` 经过 Kernel external 授权，输出“动态工具已在同一任务生效”。
- Tool record：`record:a9e81c2d22c7f5440c9a695c89c0d8a4`。
- 授权序号 33 / 35；runtime.released 43；run.settled 44。
- 中途目录命令因容器不支持 bwrap namespace 失败，原始失败保留；不以它作为目录命令成功的证据。

### 固定文档交付

- Session：`session:8b3edd2f1f6463d3f1b7d70ffff4ead7`。
- run：`session:8b3edd2f1f6463d3f1b7d70ffff4ead7:run:mu2fmrl7:2`。
- artifact：`artifact:session:8b3edd2f1f6463d3f1b7d70ffff4ead7:attempt:mu2fnli0:m`。
- record：`record:ee746a0d4f804759f99ede9cfbc68f19`。
- `document.render` 经过 Plan，生成 `report.md`，artifact endpoint 实际读取归档 47 字节；媒体类型 `text/markdown`，内容模式 `fixed`。

### GUI 插件与平台启动

- 实际加载本地 Message style template，状态“已加载 · 自动更新”。
- 单插件 Vite 构建输出 `dist/index.js`，修改模板样式后生成下一版本；同一 GUI 中加载 generation 从 1 到 2，示例编辑内容保留。
- 前一包 CLI Session：`session:22696fc3bd20b63026106ebdcebf1984`，真实 DeepSeek 对话 completed。
- TUI 在真实 PTY 显示输入界面和“就绪”，Ctrl-C 正常退出，退出码 0。
- GUI 显示 API 已连接、Agent 就绪；原生 WebView 加载 Example Domain 并关闭。

## 6. 问题与修复记录

- 前端与 Rust 并行构建超出容器内存导致退出 137：改为顺序构建，保留失败日志。
- 旧 Plan 持久化 / 客户端校验遗漏 `document.render` 与 `browser.capture`：按已确认文件修改语义补齐。
- Session bridge 遗漏 guidanceReferences / hostBinding：补齐同一命令合同。
- GUI 真实运行暴露浏览器内置工具未进入 Session 来源顺序表：补齐 browser.page/service/capture。
- Reader 双栏规则被基础样式覆盖：提高具体状态选择器优先级。
- 原生 layout/focus 可能将过期页面副本写回：仅更新该操作负责的字段，页面加载回调保持 URL/status 的事实来源。
- 受限宿主环境启动 AppKit 失败；通过已批准的正常 GUI 启动环境复验成功。

### 最终包文档与真实 diff

- 文档与原生链路验证包完成时间：2026-09-15 09:11:05 UTC。最终交付包完成时间：2026-09-15 09:29:33 UTC（北京时间 17:29:33）；版本 0.5.60，dirty 工作区构建。
- Session：`session:eb263eaa0e2af08fa2139336f3e9ec55`。
- 第一版 run `...:run:mu2gf1w4:2`、record `record:71483d6dd863ce005c5a3a1ceb716e5a`，实际创建 32 字节 Markdown。
- 第二版 run `...:run:mu2ghteq:p`、record `record:78990cac8306d7129cb4bd828149d0e1`，实际修改为 33 字节。
- CLI diff 显示 `First version` 删除、`Second version` 添加；第二版生成后，以第一版 artifactId 再次读取仍为原始 32 字节。
- 第二轮模型首次沿用了上一轮已结束 Plan，Kernel 正确拒绝；模型提出新 Plan 并完成授权后成功，拒绝记录保留。
- GUI 首次消费新 projection 时拒绝额外的 `kernelCatalogSnapshotRef`；已按现有公共合同补齐 GUI consumer，未删除该事实或放宽其他校验。

### 原生页面、截图与自身预览

- GUI 发起任务，同一 Session run：`session:eb263eaa0e2af08fa2139336f3e9ec55:run:mu2gmo6v:1g`。
- 精确页面 `preview-2`，绑定 Host `dcinstance_c8ed7b536515e5e0568ff33520cf79cbee1a1cc3d2a57ad4c99dc27c6c0b1ce6` / 窗口 `main`。
- 原生打开本地 index.html，CSS 样式生效；输入 Reviewed 并点击按钮，独立 app.js 将结果更新为 Reviewed。
- screenshot record：`record:e5a9da027d3ba504e3edb716fd074409`；71,335 字节，958 × 1642 PNG，来源范围 viewport。
- 截图由当前可见 WKWebView 生成，主对话与列表显示同一图像；随后人工将页面改为 Changed after capture，按原 artifactId 读取的截图字节保持一致。
- 关闭 preview-2 后，页面菜单不再列出该实例，截图仍在 Session 产物中。
- “预览 DeepCode”真实打开产品入口，显示同一 Session 的两份文档和截图，API 已连接；随后正常关闭。
- 最终 GUI 通过单 surface 更新并刷新加载，准确显示三份 Session 产物的降序时间。

### 开发服务生命周期

- run：`session:eb263eaa0e2af08fa2139336f3e9ec55:run:mu2gt3pv:3i`。
- Kernel `browser.service` 启动 `/usr/bin/python3 -m http.server 32185 --bind 127.0.0.1`，Host 实际持有 `development-5` / PID 61626。
- 绑定该服务创建 `preview-6`，原生页面显示正确标题与内容；服务日志记录 HTML、CSS、JS 均返回 HTTP 200。
- 显式关闭该页、停止该服务，最终 `pages: []` / `services: []`，独立连接确认 32185 端口已关闭。
- 收尾发现服务 list 的权限预览目标错误显示为 start：改为从实际 action 派生，不改变执行与授权策略。

## 7. 构建和验证边界

已完成：

- Docker protocol / Session 构建、GUI 类型检查。
- Docker daemon、CLI、TUI、host-web 编译；架构依赖检查；`git diff --check`。
- Docker GUI / Editor 构建；独立 UI 插件构建和 GUI 实际局部更新。
- macOS 两个应用包及 CLI / TUI / daemon 原生打包。
- 本次平台 CLI 的真实 Provider 对话、文档生成/修改/diff；GUI 原生工具调用通过同一 Session 与 Kernel。
- 全界面更新必须显式重新加载窗口。曾在替换资源后、尚未刷新时打开懒加载设置，触发旧 chunk 不存在；保留错误并为渲染错误页增加重新加载按钮。此路径不同于插件的局部替换，不将它表述为无需刷新。

按用户的基础验证范围未执行：

- 完整 `test.sh` 自动化测试套件与 GUI 复杂脚本矩阵。
- PDF 多页缩放/滚动恢复、默认 VS Code 打开方式、多模型逐项保存的逐项 GUI 操作矩阵。
- 等待授权、流式请求、在途工具与替换插件实现的全部交错组合；已验证的动态新增场景是同一 run 交互等待后新增 MCP，并实际执行与释放。
- D/E 的“在运行中新增浏览器能力”联合场景未单独运行；已分别验证同一 run 新增 MCP 工具，以及 GUI 明确绑定 Host 的原生浏览器执行。
- Windows / Linux 原生浏览器与打包运行；当前同页截图仅验收 macOS。其他平台的捕获实现不声明完成。
- Chromium 扩展、自动视觉审校及其他已明确后置专项。

这些未执行项不以构建成功代替。未发现需要用户改变已锁定技术路线的阻塞。

最终交付包再次通过真实 CLI Provider 往返：Session `session:914152509aa875e8a1789d002712c56c`，run `...:run:mu2k4xe0:2` completed，退出码 0。模型拒绝复述无证据的完成声明，但正常返回答复；本项证明真实对话链路，不把模型文字作为验收结果。最终 TUI 实际 PTY 启动并正常退出，GUI 显示 API 已连接、Agent 就绪；最终分类设置截图见证据目录。

## 8. 资源清理

- Docker 独立 daemon 通过 expectedIdentity 正常关闭，返回 `cleanupComplete: true`；31483 端口关闭。
- 本次 TUI 均以 Ctrl-C 正常退出，退出码 0。
- 原生页面均已显式关闭；开发进程由 Host 按本轮 serviceId 停止，32185 端口关闭。
- 最终 GUI 退出码 0；所属 daemon 的 Host descriptor 已移除，55713 端口关闭，native callback 随 Host 退出释放。
- 本轮宿主机与容器内复制的模型凭据和 Host token 已移除；用户原始配置、历史和既有服务保留。清理结果见 `evidence-20260915/cleanup.json`。

## 9. 交付物

- 本回执：[TCR-20260915-AH.md](TCR-20260915-AH.md)。
- 聚焦证据：[evidence-20260915](evidence-20260915/)，包括 CLI 回执、原生调用记录、两次真实 diff 和截图。
- 同页截图：[native-preview.png](evidence-20260915/native-preview.png)。
- 最终设置：[settings-final.jpeg](evidence-20260915/settings-final.jpeg)；Session 产物与 Reader：[session-artifacts-reader.jpeg](evidence-20260915/session-artifacts-reader.jpeg)。
- 本地目标包：`bin/macos-arm64/DeepCode-GUI.app`、`DeepCode.app`、`libexec/DeepCode-CLI`、`libexec/DeepCode-TUI`。
- 当前工作区未提交；没有创建 PR、合并、tag、push 或对外发布。

结论：A—H 主线实现已集成，按用户限定范围完成基础运行验证；上述未执行测试单独列明，不声称完整自动化测试或跨平台验收。
