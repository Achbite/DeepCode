# TCR — 分栏布局与中英文界面修订

日期：2026-09-16。延续用户已授权的前端调整；保留既有 dirty worktree，不提交 Git，不修改 Kernel/Session 事实或执行策略。

## 本轮逐项需求

| 编号 | 用户最终要求 | 实现 |
| --- | --- | --- |
| C01 | 对话信息位于主对话列顶部 | 顶栏预留与导航相同的左栏宽度，标题不再跨过左侧导航 |
| C02 | 预览展开时完成状态随主页面左移 | 标题与运行状态组成同一个会话区域，按实际 Reader 列宽收窄；与右侧标签和操作分开 |
| C03 | 右侧作为完整页面推挤主对话，放大后铺满导航右侧工作区 | 标题区与正文复用同一个 Reader 宽度；同一 Reader 实例维持并排、收起、铺满；保留左导航，展开/还原紧邻侧栏开关 |
| C04 | 最新修订：右侧预览并排展开时增加细分割线 | 原“不加右侧分隔线”由本条替代；现有拖拽分隔区域中显示 1px 主题边界，延续至顶栏，放大后不显示 |
| C05 | 中英文界面统一适用 i18n | 补齐执行过程、推理详情、代码复制、图片/图表状态、粘贴文本、插件组件示例；用户历史正文和自定义名称不改写 |
| C06 | 设置中文为“设置”，英文为“Setting” | 两种文字均接入既有 i18n 字典 |
| C07 | 删除关闭设置等按钮的蓝色包围框 | 按钮、标签和展开入口的键盘焦点改为背景/文字反馈；保留原焦点顺序与 Esc |
| C08 | 设置恢复为左下角整行入口 | 入口横向占满侧栏，整行点击；行高与导航项一致 |
| C09 | 产出标题旁不显示数字 | 侧栏卡片与折叠摘要都移除数量，文件与任务自身内容保持原文 |
| C10 | 展开预览时任务/产出继续隐藏 | 遵循用户最后澄清，维持现有隐藏行为；不改成浮窗或把任务卡片插入会话正文 |
| C11 | 总缓存命中替换底部快捷键提示并居中 | 缓存统计移到输入框下方，以单行名称和百分比显示；上下文球旁不再重复显示 |
| C12 | 参照 Apple 官方指南逐页审查，并自主修复必要布局问题 | 八个设置栏目及导航、会话、输入/工具、预览、弹层逐一检查；补充审查见 APPLE-REVIEW.md |
| C13 | 主题、字号和控件比例统一 | 语义调色板提高文字对比；浅深色共用组件；上下文球数字由 7px 改为 10px；主题选中改为填色、字重和勾选 |
| C14 | 标题稍大、下方增加留白 | 会话标题改为 14px，顶栏 48px，增加底部内边距 |
| C15 | PNG 直接显示图片，取消重复超链接 | 可预览图片仅显示缩略图，点击打开；文件名保留于悬停与无障碍标签 |
| C16 | 删除应用自身快照入口，起始页参考 Codex | “文件 / 浏览器”分为两行入口；网址输入按需展开；不再显示“预览 DeepCode” |

C03 的最新边界及 C14—C16 已完成，验证见 [后续修订回执](../preview-navigation-20260916/REVIEW.md)。以下是本表前一阶段的检查记录。

## 实施和验证范围

沿用现有组件、Host OS、设置和会话投影；标题/预览分区只使用 Reader 当前宽度和可见状态。不增加第二套状态、权限或持久化路径。

未增加、修改、删除登记测试或放宽断言。使用已有 GUI 登记测试、容器类型检查和构建，再进行本地 macOS 简单窗口操作。另补上此前锁屏未完成的插件读取、嵌套 Esc、工具/Reader 键盘检查。

## 验证结果

- 最终容器类型检查和 Vite 构建通过，GUI 资源时间为 2026-09-15 18:29:52 UTC（北京时间 09-16 02:29:52）。
- 最终 GUI 登记测试 72/72 通过，0 失败、0 跳过；实际窗口检查见 APPLE-REVIEW.md。
- 一次测试与构建并行时，协议包构建先清理 dist，导致测试找不到 @deepcode/protocol 入口（15 项加载失败）。保留 gui-tests-during-build.log；后续采用构建完成后顺序运行测试。未更改断言、fixture 或测试注册。
- 对比度计算是一次性读取设计 token 的审查，不添加测试资产；正文/辅助/状态色对四种常用背景的最低比值为浅色 4.809、深色 4.757；三种强调色与其文字/背景最低为浅色 4.810、深色 5.257，见 color-contrast.json。此计算不代表整应用无障碍认证。
