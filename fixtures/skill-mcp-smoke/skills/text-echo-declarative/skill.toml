schema_version = 1
skill_id = "fixture.text.echo"
kind = "text"
invocation_policy = "implicitAllowed"
output_policy = "textOnly"
requested_model_visible = true

[risk]
summary = "Text-only fixture. No capability grant."
declared_level = "low"
