# Product operations reference

Product behavior is documented separately from task guidance. Use `doc.read` with `name="operations.md"` for tool scope, validation, model/context accounting and settings. Use `name="execution-environments.md"` for native Windows and WSL configuration.
