/**
 * Git 面板占位组件
 * 阶段 7 接入 Git status / diff / git.tree
 */
import React from 'react';

const GitPanelPlaceholder: React.FC = () => (
  <div className="placeholder-content">
    GitPanelPlaceholder
    <div className="stage-hint">(阶段 7 接入 Git status / diff / git.tree)</div>
  </div>
);

export default GitPanelPlaceholder;
