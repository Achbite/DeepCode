{
  "name": "deepcode",
  "version": "0.5.24",
  "license": "MIT",
  "private": true,
  "description": "DeepCode - 轻量级 AI Agent 代码工作台",
  "scripts": {
    "dev": "pnpm tauri:dev:deepcode-gui",
    "dev:client:assets": "pnpm --filter @deepcode/client dev",
    "dev:deepcode-gui": "pnpm tauri:dev:deepcode-gui",
    "dev:kernel:build-only": "cargo build -p deepcode-kernel-daemon -p deepcode-host-web",
    "dev:tauri": "pnpm tauri:dev:deepcode-gui",
    "tauri:dev:deepcode-gui": "pnpm build:deepcode-gui && cargo build -p deepcode-kernel-daemon -p deepcode-host-web && pnpm --filter @deepcode/deepcode-gui-shell prepare:dist && DEEPCODE_KERNEL_DAEMON_BIN=\"$PWD/target/debug/deepcode-kernel-daemon\" DEEPCODE_HOST_WEB_BIN=\"$PWD/target/debug/deepcode-host-web\" DEEPCODE_CLIENT_DIST=\"$PWD/shells/deepcode-gui/dist\" pnpm --filter @deepcode/deepcode-gui-shell tauri:dev",
    "build": "pnpm --filter @deepcode/protocol build && pnpm --filter @deepcode/session-core build && pnpm --filter @deepcode/client build && cargo build --workspace",
    "build:client": "pnpm --filter @deepcode/client build",
    "build:deepcode-gui": "pnpm --filter @deepcode/protocol build && pnpm --filter @deepcode/session-core build && pnpm --filter @deepcode/client build:deepcode-gui",
    "build:kernel": "cargo build -p deepcode-host-web",
    "build:tauri": "pnpm --filter @deepcode/tauri-shell tauri:build",
    "build:tauri:deepcode-gui": "pnpm build:deepcode-gui && pnpm --filter @deepcode/deepcode-gui-shell prepare:dist && pnpm --filter @deepcode/deepcode-gui-shell tauri:build",
    "start": "pnpm tauri:dev:deepcode-gui",
    "typecheck": "pnpm --filter @deepcode/protocol typecheck && pnpm --filter @deepcode/session-core typecheck && pnpm --filter @deepcode/client typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
